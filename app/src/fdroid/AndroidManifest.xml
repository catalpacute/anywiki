<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application>
        <meta-data
            android:name="@string/preference_key_app_channel"
            android:value="F-Droid"
            tools:replace="android:value" />

        <meta-data android:name="com.google.android.gms.wallet.api.enabled"
            tools:node="remove"/>

        <service
            android:name="org.wikipedia.push.WikipediaFirebaseMessagingService"
            tools:node="remove"/>

        <activity
            android:name="org.wikipedia.donate.GooglePayActivity"
            tools:node="remove"/>

    </application>

</manifest>

